# audiblez-mlx dependencies (torch / Kokoro is the default engine).
#
# This mirrors pyproject.toml: the core deps (which include kokoro) plus the [edge]
# extra. The accelerated MLX engine is an opt-in extra for Apple Silicon only
# (`pip install ".[mlx]"`); mlx-audio only installs on darwin/arm64, so it is not
# listed here. For the desktop GUI add the [gui] extra separately.
#
#   pip install -r requirements.txt
#
# The [gui] extra (wxpython, pillow) is intentionally omitted; add it separately if
# you want the desktop UI:  pip install ".[gui]"

# --- core (torch / Kokoro is the default engine) ---
ebooklib>=0.18,<0.19
soundfile>=0.13.1,<0.14.0
pick>=2.4.0,<3.0.0
beautifulsoup4>=4.12,<5.0
spacy>=3.8.3,<4.0.0
misaki[en,zh]>=0.9.2
opencc-python-reimplemented>=0.1.7,<0.2.0
tabulate>=0.9.0,<0.10.0
kokoro==0.9.4

# --- [edge] extra: Microsoft's online TTS (needs network at synthesis time) ---
edge-tts>=7.2.0
