# audiblez-mlx dependencies for a non-Apple machine (PC / Linux).
#
# This mirrors pyproject.toml: the core deps plus the [torch] and [edge] extras.
# On Apple Silicon prefer `pip install .` (or `pip install ".[edge]"`) so the MLX
# backend is used; mlx-audio only installs on darwin/arm64, so it is not listed here.
#
#   pip install -r requirements.txt
#
# The [gui] extra (wxpython, pillow) is intentionally omitted; add it separately if
# you want the desktop UI:  pip install ".[gui]"

# --- core ---
ebooklib>=0.18,<0.19
soundfile>=0.13.1,<0.14.0
pick>=2.4.0,<3.0.0
bs4>=0.0.2,<0.0.3
spacy>=3.8.3,<4.0.0
misaki[en,zh]>=0.9.2
opencc-python-reimplemented>=0.1.7,<0.2.0
tabulate>=0.9.0,<0.10.0

# --- [torch] extra: portable Kokoro backend (required on non-Apple machines) ---
kokoro==0.9.4

# --- [edge] extra: Microsoft's online TTS (needs network at synthesis time) ---
edge-tts>=7.2.0
